// Re-exported from the shared SDK so the seat status enum has a single source of
// truth across services and the frontend (see API-CONTRACT.md §3, DECISIONS.md ADR-8).
export { SeatStatus } from '@cinema/internal-sdk';
