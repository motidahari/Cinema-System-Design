// Re-exported from the shared SDK so the reservation status enum has a single
// source of truth across services and the frontend (DECISIONS.md ADR-8).
export { ReservationStatus } from '@cinema/internal-sdk';
